var Fin = require('fs');
var Path = require('path');
const utf8 = "utf8"

var ReadFile = function (Path) {
	return Fin.readFileSync(Path, utf8);
}

var isGraphAvailable = (graphName)=>{

}

var isLanguageSupport = (graphName)=>{
    
}